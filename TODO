DONE
class => Class.create
rewrite "as"
rewrite "is"
rewrite methods
mangle protected names
function typename stripping
var typename stripping
ensure we can get to 'sp'
rewrite implicit pseudo-this. calls on methods
optional arguments
splat arguments
arguments instanceof Array
arguments.callee
rewrite constructors
mangle private names
rewrite simple properties
static methods
rewrite getters and setters
rewrite into package
inheritence
for-each-in loops
try-catch
static properties
static name references
class name resolution
method overrides

TODAY
inheritence name resolution
implicit globals from flash.utils (e.g. flash.utils.setTimeout => setTimeout)
rewrite imported name references

TOMORROW
class visibility handling
auto-insert .bind
AS3 coersion with assignment
check keyword collisions

TWO DAYS FROM NOW
fn.bind
interfaces
investigate debugging assistants (stacktrace rewriting, e.g.)
super()
with blocks
